adapter:
  type: pusht

state_alignment:
  # state_align: configuration[0:6]
  supervised_indices: [0, 1, 2, 3, 4, 5]
  velocity_indices: []

dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state
