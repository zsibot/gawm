adapter:
  type: tworoom

state_alignment:
  # state_align: configuration[0:2]
  supervised_indices: [0, 1]
  velocity_indices: []

dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: tworoom
  keys_to_load:
    - pixels
    - action
    - proprio
  keys_to_cache:
    - action
    - proprio
